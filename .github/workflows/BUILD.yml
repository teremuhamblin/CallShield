name: Build

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build placeholder
        run: |
          echo "TODO: ajouter build Android/iOS/Backend"
